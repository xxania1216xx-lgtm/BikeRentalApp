<UserControl x:Class="BikeRentalApp.Views.UserDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BikeRentalApp"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900" Background="{StaticResource BackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Windows 11 style floating header -->
        <Border Background="{StaticResource SurfaceBrush}" Padding="20,15" Margin="10,10,10,0" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#20000000" Opacity="0.1"/>
            </Border.Effect>
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Witaj, " FontSize="20"/>
                    <TextBlock Text="{Binding User.UserName}" FontSize="20" FontWeight="SemiBold"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Saldo: " VerticalAlignment="Center" FontSize="16" Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding User.Balance, StringFormat='{}{0:N2} zł'}" VerticalAlignment="Center" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,20,0"/>
                    <Button Content="Wyloguj" Command="{Binding LogoutCommand}" Style="{StaticResource ModernButton}" Background="#FF5F5F" Padding="15,8" FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>
        </Border>

        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0" Margin="10">
            <TabItem Header="Wypożycz/Zwróć">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Margin="0,0,10,0">
                        <TextBlock Text="Wybierz stację wypożyczenia" Style="{StaticResource LabelBlock}"/>
                        
                        <!-- Nowoczesne menu stacji zamiast comboboxa -->
                        <ListBox ItemsSource="{Binding Stations}" SelectedItem="{Binding SelectedStation}" Background="Transparent" BorderThickness="0" Margin="0,5,0,15" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="15,10"/>
                                    <Setter Property="Margin" Value="0,0,10,10"/>
                                    <Setter Property="Background" Value="White"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                        <Setter Property="Foreground" Value="White"/>
                                                        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                    </Trigger>
                                                    <MultiTrigger>
                                                        <MultiTrigger.Conditions>
                                                            <Condition Property="IsMouseOver" Value="True"/>
                                                            <Condition Property="IsSelected" Value="False"/>
                                                        </MultiTrigger.Conditions>
                                                        <Setter Property="Background" Value="#F0F8FF"/>
                                                    </MultiTrigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding StationName}" FontWeight="SemiBold" FontSize="14"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        
                        <TextBlock Text="Dostępne rowery" Style="{StaticResource LabelBlock}" Margin="0,10,0,5"/>
                        <ListBox ItemsSource="{Binding AvailableBikes}" SelectedItem="{Binding SelectedBike}" Height="200" DisplayMemberPath="Model"/>
                        
                        <TextBlock Text="{Binding SelectedBike.PricePerHour, StringFormat='Cena za godzinę: {0:N2} zł'}" 
                                   Margin="0,10,0,0" FontWeight="Bold" FontSize="14" Foreground="{StaticResource PrimaryBrush}"
                                   Visibility="{Binding SelectedBike, Converter={StaticResource NullToVisibilityConverter}}"/>

                        <Button Content="Wypożycz wybrany rower" Command="{Binding RentCommand}" Style="{StaticResource ModernButton}" Margin="0,10,0,0"/>
                        
                        <TextBlock Text="{Binding StatusMessage}" Margin="0,10,0,0" Foreground="#FF5F5F" FontWeight="Bold" TextWrapping="Wrap"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="Twoje aktualne wypożyczenie" Style="{StaticResource HeaderBlock}" FontSize="18"/>
                        <Border Background="White" Padding="20" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="{Binding CurrentRental.Bike.Model, FallbackValue='Brak aktywnego wypożyczenia'}" FontWeight="Bold" FontSize="16"/>
                                <TextBlock Text="{Binding CurrentRental.StartTime, StringFormat='Od: {0:g}'}" Margin="0,5,0,0" Visibility="{Binding CurrentRental, Converter={StaticResource NullToVisibilityConverter}}"/>
                                
                                <!-- Dynamic Timer -->
                                <TextBlock Text="{Binding ElapsedTime}" Margin="0,10,0,0" FontSize="16" FontWeight="SemiBold" Foreground="#FF5F5F" Visibility="{Binding CurrentRental, Converter={StaticResource NullToVisibilityConverter}}"/>

                                <!-- Return Station Selection -->
                                <TextBlock Text="Wybierz stację zwrotu:" Style="{StaticResource LabelBlock}" Margin="0,15,0,5" Visibility="{Binding CurrentRental, Converter={StaticResource NullToVisibilityConverter}}"/>
                                <ComboBox ItemsSource="{Binding Stations}" SelectedItem="{Binding ReturnStation}" DisplayMemberPath="StationName" Padding="10,5" Visibility="{Binding CurrentRental, Converter={StaticResource NullToVisibilityConverter}}"/>
                                
                                <Button Content="Zwróć rower" Command="{Binding ReturnCommand}" Style="{StaticResource ModernButton}" Background="#4CAF50" Margin="0,15,0,0" Visibility="{Binding CurrentRental, Converter={StaticResource NullToVisibilityConverter}}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Historia">
                <DataGrid ItemsSource="{Binding RentalHistory}" AutoGenerateColumns="False" IsReadOnly="True" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Rower" Binding="{Binding Bike.Model}" Width="*"/>
                        <DataGridTextColumn Header="Start" Binding="{Binding StartTime, StringFormat='{}{0:g}'}" Width="Auto"/>
                        <DataGridTextColumn Header="Koniec" Binding="{Binding EndTime, StringFormat='{}{0:g}'}" Width="Auto"/>
                        <DataGridTextColumn Header="Stacja Start" Binding="{Binding StartStation.StationName}" Width="*"/>
                        <DataGridTextColumn Header="Stacja Koniec" Binding="{Binding EndStation.StationName}" Width="*"/>
                        <DataGridTextColumn Header="Koszt" Binding="{Binding TotalCost, StringFormat='{}{0:N2} zł'}" Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Konto">
                <Grid>
                    <Border Background="White" CornerRadius="8" Margin="20" MaxWidth="700" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="40" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="15" ShadowDepth="4" Opacity="0.05"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="Zarządzanie kontem" Style="{StaticResource HeaderBlock}" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="40"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Lewa strona: Doładowanie -->
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Doładuj konto (PLN)" Style="{StaticResource LabelBlock}" FontSize="14"/>
                                    <TextBox Text="{Binding RechargeAmount, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" FontSize="18" Padding="15,10" Margin="0,0,0,10"/>
                                    <Button Content="Doładuj środki" Command="{Binding RechargeCommand}" Style="{StaticResource ModernButton}" FontSize="16" Padding="15,12"/>
                                </StackPanel>

                                <!-- Prawa strona: Zmiana hasła -->
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Zmiana hasła" Style="{StaticResource LabelBlock}" FontSize="14"/>
                                    <TextBox Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" FontSize="18" Padding="15,10" Margin="0,0,0,10"/>
                                    <Button Content="Zmień hasło" Command="{Binding ChangePasswordCommand}" Style="{StaticResource ModernButton}" FontSize="16" Padding="15,12"/>
                                </StackPanel>
                            </Grid>
                            
                            <TextBlock Text="{Binding StatusMessage}" Margin="0,30,0,0" Foreground="{StaticResource PrimaryBrush}" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>