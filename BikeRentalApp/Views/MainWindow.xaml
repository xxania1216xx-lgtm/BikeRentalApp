<Window x:Class="BikeRentalApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:BikeRentalApp.Views"
        xmlns:viewmodels="clr-namespace:BikeRentalApp.ViewModels"
        mc:Ignorable="d"
        Title="Bike Rental App" Height="600" Width="900"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:LoginViewModel}">
            <views:LoginView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:UserDashboardViewModel}">
            <views:UserDashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:AdminDashboardViewModel}">
            <views:AdminDashboardView/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <ContentControl Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>
