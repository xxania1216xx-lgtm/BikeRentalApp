<UserControl x:Class="BikeRentalApp.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BikeRentalApp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Border Background="{StaticResource SurfaceBrush}" Padding="40" CornerRadius="8" Width="350">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.1"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Bike Rental" Style="{StaticResource HeaderBlock}" HorizontalAlignment="Center"/>
                
                <TextBlock Text="Login" Style="{StaticResource LabelBlock}"/>
                <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                
                <TextBlock Text="Hasło" Style="{StaticResource LabelBlock}"/>
                <PasswordBox x:Name="PasswordInput" PasswordChanged="PasswordInput_PasswordChanged" Style="{StaticResource ModernPasswordBox}"/>
                
                <Button Content="Zaloguj się" Command="{Binding LoginCommand}" Margin="0,30,0,10" Style="{StaticResource ModernButton}"/>
                <Button Content="Załóż konto" Command="{Binding RegisterCommand}" Style="{StaticResource ModernButton}" Background="Transparent" Foreground="{StaticResource PrimaryBrush}"/>
                
                <TextBlock Text="{Binding StatusMessage}" Margin="0,20,0,0" Foreground="Red" HorizontalAlignment="Center" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
